package Inheritance;

public class BoxColor extends BoxWeight {
    // Here Box weight is parent class for both box-price and box-color child class ans it is known as hierarchical inheritance
    // Multiple inheritance is not allowed in java

    // Hybrid inheritance means mix of single and hierarchical inheritance
    // A is parent for B and C and these both are parent class for D

    // Inheriting itself is not allowed means we can't write BoxColor extends BoxColor
}
